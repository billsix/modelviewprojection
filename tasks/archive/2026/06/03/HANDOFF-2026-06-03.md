# HANDOFF 2026-06-03

Session focus: finished the **Cayley-graph visualization** arc and **moved the
mvpVisualization demos into the package**. Everything below is done + committed
(except the loose ends in the last section).

## End state

- **The 7 viz demos now live in the package** at
  `src/modelviewprojection/mvpvisualization/<name>.py` (model, pushmatrix,
  modelview, modelview2d, modelvieworthoprojection, modelviewperspectiveprojection,
  coordinatesystems). The old root-level `mvpVisualization/` is gone.
  - Run them **by path**: `python src/modelviewprojection/mvpvisualization/<name>.py`
    (not `python -m`). Clean absolute package imports, no `sys.path` preludes.
  - Engine modules in the same package: `cayley_gl` (GL toolkit, mechanism-only),
    `cayleygraph` (immutable DAG), `cayleyscene` (animation/timeline + `to_matrix`),
    `_pipeline` (shaders/VAOs).
  - **Shaders load demo-relative** (demo21/22 idiom): each demo computes
    `pwd = os.path.dirname(os.path.abspath(__file__))` and calls
    `cayley_gl.build_standard(shader_dir=pwd, ...)`; `_SHARED_DIR` is gone. Layout
    stays flat (shaders shared in the package dir).
- **UI**: controls live in a top **menubar** (File / Playback / Camera / View From /
  View / Scene), the two function-composition trees in a "Cayley Graph" panel toggled
  by View → Show Graph; keyboard: Esc quit, F11 fullscreen, SPACE pause, R restart,
  G graph, WASD camera, N (2d NDC zoom).
- **Math/engine**: `InvertibleFunction` is interpolable (`at(t)`/`steps()`); demos
  drive `pyMatrixStack` directly; `Frustum` (perspective) vs `RectangularPrism` (ortho).
- **Book**: ch10/17/19 prose updated to the new paths; stale ch17 callouts repointed.
- **Tests**: 85 headless tests pass via `modelviewprojection.mvpvisualization`; ruff clean.

## Durable records (archived)

- `tasks/archive/2026/06/03/cayley-graph-visualizations.md` — consolidated end-state
  summary of the whole Cayley arc (datastructure, demos, UI, decisions).
- `tasks/archive/2026/06/03/move-mvpvis-demos.md` — the package move + shader_dir
  follow-up.

## Open / next session

1. **Visual verification (the one real open item).** The move, the import rewrite,
   and the demo-relative `shader_dir` change are **unverified on screen** (no GL in the
   container). Run each demo by path and confirm shaders resolve + it renders:
   `python src/modelviewprojection/mvpvisualization/<name>.py`. Start with
   `modelvieworthoprojection.py` (newest squash + RectangularPrism path).
2. **`.gitignore` the runtime `imgui.ini`** (repo root, `ports/`, `ports/openglsuperbiblev4/`)
   and possibly `output/` — they're regenerated on every run and keep showing as untracked.
3. **Pre-existing ruff nit, parked:** `ports/openglsuperbiblev4/_common.py` has an
   unsorted-import (I001) — that ports tree isn't lint-clean and was out of scope here.
4. One uncommitted edit: the follow-up note in the archived `move-mvpvis-demos.md`.

## In-flight tasks untouched this session
`tasks/`: book-code-drift-ch7-15, book-code-drift-ch16-21, finish-pdf-epub-build,
ports-pbo-floattex-runtime-crashes, shadowmap-depth-discrimination.
