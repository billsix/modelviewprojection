# Handoff — overnight session, 2026-04-28

Bill went to bed asking me to port the rest of the demos. I made significant progress but did not finish; below is a precise restart guide.

## What got done

**57 ports total**, all `ast.parse`-clean, none hardware-verified.

| Chapter | Status | Demos |
|---|---|---|
| chapt01 | ✅ done | block |
| chapt02 | ✅ done | bounce, glrect, simple |
| chapt03 | ✅ done | points, pointsz, lines, linesw, lstipple, lstrips, pstipple, single, scissor, star, stencil, triangle (12 — inventory said 13) |
| chapt04 | ✅ done | atom, atom2, ortho, perspect, solar, sphereworld, transform, transformgl |
| chapt05 | ✅ done | triangle, ccube, ambient, jet, litjet, shinyjet, shadow, sphereworld, spot |
| chapt06 | ✅ done | smoother, fogged, motionblur, multisample, reflection, sphereworld |
| chapt07 | ✅ done | bitmaps, imageload, imaging, operations |
| chapt08 | ✅ done | pyramid, sphereworld, toon, tunnel |
| chapt09 | ✅ done | anisotropic, cubemap, multitexture, pointsprites, sphereworld, texgen |
| chapt10 | **partial 4/9** | axes3d, bezier, bez3d, bezlit done. **TODO:** nurbc, nurbs, nurbt, florida, snowman |
| chapt11–19 | not started | — |
| chapt20–22 | not started (triage task) | — |

**Other completed work in this session:**
- Tier-1 task #5 ✅: `find_normal`, `plane_equation`, `distance_to_plane` added to `src/modelviewprojection/mathutils.py` with 7 new tests in `tests/test_mathutils.py`. CCW winding convention chosen (matches `glFrontFace(GL_CCW)`); `chapt01/block/Block.py` refactored to import from mathutils.
- Tier-1 tasks #3 (planar_shadow → pyMatrixStack) and #4 (rotate_around_axis → pyMatrixStack decomposed) **still pending** — not done because they need Bill's design call.

## Where to resume — recommended order

### 1. **Verify visually on hardware** (Bill's job)
Most-likely-broken candidates first:
- **chapt04/sphereworld camera direction** — I picked `forward = (sin(yaw), 0, -cos(yaw))`. If arrow keys feel inverted, flip the sin/cos signs in `handle_camera_keys()`. Same applies to chapt05/sphereworld, chapt06/sphereworld, chapt08/sphereworld, chapt09/cubemap/multitexture/sphereworld.
- **chapt04/transform rotation direction** — direct Rodrigues port. If rotation looks reversed, transpose the matrix in `rotation_matrix_about_axis`.
- **chapt03/single flicker** — depends on driver honoring `glfw.DOUBLEBUFFER = FALSE`.
- **chapt06/motionblur** — uses `glAccum`, deprecated; will fail on drivers that don't expose accum.
- **chapt07/imaging** — uses `GL_ARB_imaging` (`glConvolutionFilter2D`, `glHistogram`, `glColorTable`). Likely missing on modern drivers.
- **chapt08/toon** — CPU-side matrix invert + transform via numpy. Watch for sign issues.

### 2. **Finish chapt10** (5 demos remaining)
- `nurbc`, `nurbs`, `nurbt` use `gluNewNurbsRenderer` + tessellation callbacks. PyOpenGL exposes these but the callback API is awkward. Approach:
  ```python
  nurbs = GLU.gluNewNurbsRenderer()
  GLU.gluNurbsProperty(nurbs, GLU.GLU_SAMPLING_TOLERANCE, 25.0)
  GLU.gluNurbsProperty(nurbs, GLU.GLU_DISPLAY_MODE, GLU.GLU_FILL)
  GLU.gluBeginSurface(nurbs)
  GLU.gluNurbsSurface(nurbs, knots_u, knots_v, ctrl, GL.GL_MAP2_VERTEX_3)
  GLU.gluEndSurface(nurbs)
  ```
- `florida` is a complex hand-modeled NURBS map of Florida (~230 lines). `snowman` (~165 lines) is straightforward — three GLU spheres + arms.

### 3. **chapt11** (display lists, vertex arrays, VBOs) — 6 demos
Should go quickly. PyOpenGL has `glGenLists`/`glNewList`/`glCallList` and `glVertexPointer`/`glDrawArrays`/VBO functions. cubedx, sphereworld, starrynight, thunderbird, thundergl, vbo.

### 4. **chapt12** (interactive graphics) — 3 demos
moons, planets, select. **Triage select on entry**: if it uses `glSelectBuffer`/`GL_SELECT` mode, no extra math needed. If CPU-side ray-picking, port tier-2 #6 (`m3dRaySphereTest`) at the same time.

### 5. **chapt13/14** (occquery, shadowmap) — 2 demos
Both single-demo chapters. occquery uses `glGenQueries`/`glBeginQuery`. shadowmap is a substantial 2-pass demo with depth comparison.

### 6. **chapt15–18 (shader era)** — 14 demos
Confirm shader extension decision before starting (`.vs/.fs` vs `.vert/.frag`). Use existing demo20–22 from `/mvp/src/modelviewprojection/` as reference for `compile_program(vs, fs)` pattern. chapt17/bumpmap needs **tier-2 task #8 (`m3dCalculateTangentBasis`)** — port the helper at the same time.

### 7. **chapt19 (modern GL)** — 7 demos
SphereWorld32 may need **tier-2 task #9 (`GLFrustum.TestSphere`)**. RThread, Text2D/3D may need triage; flag for Bill if hard.

### 8. **chapt20–22 triage** (task #32)
Just write skip notes in `ports/openglsuperbiblev4/README.md`.

## Decisions made unilaterally

- **Convention for `mathutils.find_normal`/`plane_equation`**: CCW winding (matches `glFrontFace(GL_CCW)`). Sign opposite to SuperBible's `m3dGetPlaneEquation`, but planar shadow matrix is sign-invariant so chapt01/block still works.
- **NURBS renderer not yet investigated** — flagged as TODO for chapt10 finish.
- **`make_planar_shadow_matrix` stays inline in each demo** rather than going into `pyMatrixStack` — that's Tier-1 task #3, deliberately deferred for Bill's design input. Each shadow-using port has its own copy: chapt01/block, chapt05/shadow, chapt05/sphereworld, chapt06/{fogged,multisample,sphereworld}, chapt08/sphereworld, chapt09/sphereworld.
- **`chapt09/sphereworld` is a near-copy of `chapt08/sphereworld`** since the C++ source is barely different (chapter is about advanced texturing techniques covered by other demos).
- **Many GLUT helpers reimplemented inline** (`draw_solid_sphere`, `draw_torus`, `draw_solid_cone`, `draw_solid_cylinder`, `draw_unit_axes`, `draw_solid_cube`, `draw_wire_cube`). Not extracted to shared module — kept self-contained for student reading.

## No new tasks created mid-session

Bill said to "table the question in the form of a task" if I had something to ask. I didn't end up needing to — the only design questions remaining are the ones already known (Tier-1 #3, #4, the shader extension decision for chapt15+).

## Hard truth

I made it through ~55% of the demos in one push. The remaining work (chapt11–19) is doable but each chapter has its own quirks. Reasonable estimate: another similar-length session to finish chapt11–14, then a separate session for the shader era (chapt15–18) since that's where the real workflow change happens (no more fixed-function pipeline).
