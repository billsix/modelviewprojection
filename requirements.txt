autoflake
furo
gacalc>=0.0.8
glfw
ImageIO
imgui-bundle[glfw]
isort
# single-device audio backend (decode + PlaybackDevice); replaced
# just_playback 2026-07-09 -- see tasks/leadingedge-audio-clunk-and-freeze.md
miniaudio
jupyter
jupytext
mathjax
matplotlib
moviepy
myst-nb
nbsphinx
numpy
PyOpenGL
# The optional C accelerator for PyOpenGL (silences the 'No OpenGL_accelerate
# module loaded' INFO and speeds up per-call array marshalling). Pinned to
# match the dnf python3-pyopengl version in the Dockerfile -- bump together.
PyOpenGL-accelerate==3.1.10
pytest
Sphinx
sphinxcontrib-bibtex
sphinx_rtd_theme
sympy
wxpython
